setwd("C:\\Users\\Admin\\Desktop\\Моя прелесть\\TV\\921\\17")
v = read.csv("r2z1.csv")
x = v[[1]]
y = v[[2]]
alpha = 0.05
n = length(x)
u = y - x # Разность выборок
mean_u = sum(u) / n # Среднее значение выборки
dd = sum((u - mean_u)^2) / n # Смещенная дисперсия выборок
ndf = n - 1 # Число степеней свободы
Ccr = qt(1 - alpha, ndf) # С-критическая константа
T = mean_u * sqrt(n - 1) / sqrt(dd) # Статистика одновыборочного критерия Стьюдента
#Статистика меньше критической константы значит принимается нулевая гипотеза
p = 1 - pt(T, ndf) # p-значение
"Критерий размера alpha отвергает нулевую гипотезу тогда и только тогда, когда p-значение p <= alpha"
"В нашем случае p > alpha - хорошее согласие с нулевой гипотезой"
